import { Board } from '@/components/board/Board';

export default function BoardPage() {
  return (
    <main className="min-h-screen bg-jira-page-light dark:bg-jira-page-dark !overflow-hidden">
      <Board />
    </main>
  );
} 